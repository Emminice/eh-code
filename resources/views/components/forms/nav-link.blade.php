{{-- @props(['active' => false])

<button class="nav-link active block rounded-md bg-gray-900 px-4 py-2 text-base font-medium text-white" style="{{ $active ? 'font-weight: bold; border-radius: 8px; background-color: rgba(0, 128, 0, 0.316);': 'font-weight: bold; border-radius: 8px;' }}" aria-current="{{ $active ? 'page': 'false' }}"{{ $attributes }}
>{{ $slot }}</button> --}}

